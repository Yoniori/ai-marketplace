# ─── Vibe Code Market — Agentic Crew dependencies ───────────────────────────
# Python ≥ 3.10 required by crewai.

crewai>=1.0.0,<2.0.0
crewai-tools>=0.14.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# LiteLLM is pulled transitively by crewai and routes to both Anthropic and
# OpenAI, so no separate provider SDK is required.
